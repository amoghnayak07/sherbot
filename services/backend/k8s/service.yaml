apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  namespace: demo
  labels:
    app: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 8001
      targetPort: 8001
